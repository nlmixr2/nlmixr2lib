% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/addSecondAbsorption.R
\name{addSecondAbsorption}
\alias{addSecondAbsorption}
\title{Add a second absorption path for double-absorption models}
\usage{
addSecondAbsorption(
  ui,
  type = c("first", "zero"),
  delay = c("none", "lag", "transit"),
  n = NULL,
  central = "central",
  depot = "depot",
  depot2 = "depot2",
  ka = "ka",
  ka2 = "ka2",
  tk0 = "tk0",
  f1 = 0.7
)
}
\arguments{
\item{ui}{The model as a function (or something convertible to an rxUi
object)}

\item{type}{second-path input type: `"zero"` for a zero-order
(modeled-duration) input or `"first"` for a first-order (`ka2`)
input}

\item{delay}{second-path delay: `"none"`, `"lag"` (an `alag` on the
second path), or `"transit"` (a transit chain feeding the second
path)}

\item{n}{number of transit compartments when `delay = "transit"`}

\item{central}{central compartment name}

\item{depot}{depot compartment name}

\item{depot2}{name of the second depot compartment}

\item{ka}{absorption rate parameter name}

\item{ka2}{name of the second first-order absorption rate}

\item{tk0}{name of the zero-order duration on the second path}

\item{f1}{initial fraction of the dose entering the first path, in
(0,1)}
}
\value{
a model with two parallel absorption paths
}
\description{
Adds a parallel absorption path (`depot2`) to a model that already
has first-order absorption through `depot`, for Monolix-style
double-absorption models: one dose record is split at translation
time into two parallel inputs with a logit-parameterized `F1`
apportionment.  The second path can itself be zero-order
(`type = "zero"`, a modeled `dur(depot2)` input), first-order
(`type = "first"`, `ka2`), or a transit chain (`delay = "transit"`),
with an optional lag.
}
\details{
Only one second absorption path is supported per model; applying
this twice raises an error.
}
\examples{

# simultaneous zero-order + first-order (Monolix double absorption)
readModelDb("PK_1cmt_des") |>
  addSecondAbsorption(type = "first", delay = "lag", f1 = 0.7)

}
\seealso{
Other absorption:
\code{\link[=addTransit]{addTransit()}},
\code{\link[=addWeibullAbs]{addWeibullAbs()}},
\code{\link[=addZeroOrderAbs]{addZeroOrderAbs()}},
\code{\link[=convertAbsForceLongerDelay]{convertAbsForceLongerDelay()}},
\code{\link[=convertAbsSequential]{convertAbsSequential()}},
\code{\link[=removeTransit]{removeTransit()}},
\code{\link[=removeZeroOrderAbs]{removeZeroOrderAbs()}}
}
\author{
Matthew L. Fidler
}
\concept{absorption}
