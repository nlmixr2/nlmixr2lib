% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/addPeriph.R
\name{addPeriph}
\alias{addPeriph}
\title{Add a peripheral compartment to a model}
\usage{
addPeriph(ui, n = NULL, central = "central", model)
}
\arguments{
\item{ui}{The model as a function (or something convertible to an rxUi
object)}

\item{n}{which peripheral to add: 1 (`peripheral1`, `q`, `vp`) or 2
(`peripheral2`, `q2`, `vp2`); defaults to the first missing one}

\item{central}{central compartment name}

\item{model}{Deprecated alias for \code{ui}. Supplying \code{model}
instead of \code{ui} still works but emits a deprecation warning.}
}
\value{
a model with the peripheral compartment added
}
\description{
Adds a peripheral distribution compartment (`peripheral1`, then
`peripheral2`) to a central compartment, following the
`PK_2cmt_des`/`PK_3cmt_des` seed conventions: intercompartmental
clearance `q` (`q2` for the second) and peripheral volume `vp`
(`vp2`), with `k12 <- q/vc`, `k21 <- q/vp` (and `k13`/`k31` for the
second).  At most two peripheral compartments are supported,
matching the seeds.
}
\examples{

readModelDb("PK_1cmt_des") |> addPeriph()

readModelDb("PK_2cmt_des") |> addPeriph()

}
\seealso{
Other distribution:
\code{\link[=removePeriph]{removePeriph()}}
}
\author{
Matthew L. Fidler
}
\concept{distribution}
