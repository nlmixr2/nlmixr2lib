% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ncaComparison.R
\name{ncaParamLabel}
\alias{ncaParamLabel}
\title{Map PKNCA parameter codes to friendly human-readable labels}
\usage{
ncaParamLabel(code, units = NULL)
}
\arguments{
\item{code}{Character vector of PKNCA parameter codes. `NA` values are
passed through unchanged.}

\item{units}{Optional named character vector keyed by PKNCA code. For
each code with a matching entry, the friendly label is suffixed with
the unit string in parentheses (e.g. `"Cmax (ng/mL)"`).}
}
\value{
Character vector the same length as `code`.
}
\description{
Vectorized recoder that maps the PKNCA parameter codes (`PPTESTCD`
values such as `"cmax"`, `"aucinf.obs"`, `"half.life"`) to the friendly
pharmacometric labels readers expect in tables and prose (`"Cmax"`,
`"AUC0-Inf (obs)"`, `"t1/2"`). Codes that are not in the mapping
table are returned unchanged with a warning, so unfamiliar parameters
never get silently dropped.
}
\examples{
ncaParamLabel(c("cmax", "tmax", "auclast", "aucinf.obs", "half.life"))
ncaParamLabel(
  c("cmax", "auclast"),
  units = c(cmax = "ng/mL", auclast = "ng*h/mL")
)
}
\author{
Bill Denney
}
