% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/addPeriph.R
\name{removePeriph}
\alias{removePeriph}
\title{Remove a peripheral compartment from a model}
\usage{
removePeriph(ui, n = NULL, central = "central", model)
}
\arguments{
\item{ui}{The model as a function (or something convertible to an rxUi
object)}

\item{n}{which peripheral to remove: 1 or 2; defaults to the
highest-numbered one present}

\item{central}{central compartment name}

\item{model}{Deprecated alias for \code{ui}. Supplying \code{model}
instead of \code{ui} still works but emits a deprecation warning.}
}
\value{
a model with the peripheral compartment removed
}
\description{
Removes `peripheral2` (or `peripheral1` when it is the only one),
rewiring the central compartment and dropping the associated
parameters.  `peripheral1` cannot be removed while `peripheral2` is
still present.
}
\examples{

readModelDb("PK_2cmt_des") |> removePeriph()

}
\seealso{
Other distribution:
\code{\link[=addPeriph]{addPeriph()}}
}
\author{
Matthew L. Fidler
}
\concept{distribution}
