% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/addProp.R
\name{addBioavailabilityLogit}
\alias{addBioavailabilityLogit}
\title{Add a logit-parameterized bioavailability to a compartment}
\usage{
addBioavailabilityLogit(ui, cmt, cmt2 = NULL, f = 0.8)
}
\arguments{
\item{ui}{rxode2 ui object}

\item{cmt}{compartment to apply the bioavailability to; in the
dose-split form this compartment receives the remainder
\code{1 - f<Depot>}}

\item{cmt2}{optional second compartment; when supplied the logit
fraction is estimated for this compartment (which receives the
full \code{f<Depot2>}) while \code{cmt} receives the remainder}

\item{f}{initial bioavailability fraction, in (0,1), or NULL to
leave the initial estimate unset}
}
\value{
rxode2 ui object with the logit bioavailability applied
}
\description{
Adds a bioavailability fraction constrained to (0,1) with a
logit/expit parameterization, matching Monolix-style \code{F}
(oral, subcutaneous) and the \code{F1}/\code{Fd} dose-apportionment
fractions.
}
\details{
Single-compartment form (no \code{cmt2}): the fraction is estimated
for \code{cmt} as \code{f(cmt) <- f<Depot>} with \code{f<Depot> <-
  expit(logitf<Depot>)}.

Dose-split form (two compartments): the fraction for \code{cmt} is
\code{f(cmt) <- 1 - f<Depot2>} with \code{f<Depot2> <-
  expit(logitf<Depot2>)}, so the named \emph{first} compartment keeps
only the remainder while the fraction itself is labeled for the
second compartment.  This matches the double-absorption \code{F1}
convention where the split fraction is named for (and estimated on)
the second absorption path; see the \code{PK_double_sim} library
seeds, which estimate \code{fdepot1}/\code{lgfdepot1} on the first
path.

Unlike [addBioavailabilityLog()], which log-parameterizes \code{F}
unboundedly, the logit form keeps \code{F} in (0,1): the initial
estimate is stored on the logit scale and the model block defines
the \code{expit()} line, which keeps the mu-referenced bounds.
}
\examples{

readModelDb("PK_1cmt_des") |> addBioavailability(depot, scale = "logit")

# dose split between two absorption paths, fraction named for depot2
readModelDb("PK_1cmt_des") |>
  addDepot(depot = "depot2", ka = "ka2") |>
  addBioavailability(depot2, depot, scale = "logit")

}
\seealso{
Other absorption:
\code{\link[=addTransit]{addTransit()}},
\code{\link[=addWeibullAbs]{addWeibullAbs()}},
\code{\link[=removeTransit]{removeTransit()}}
}
\author{
Matthew L. Fidler
}
\concept{absorption}
