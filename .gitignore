inst/doc
.Rhistory
.RData
.Ruserdata
src/*.o
src/*.so
src/*.dll
src/Makevars
src/dbver.h
*.dll
*.Rout
*.Rcheck/
*.tar.gz
*.tgz
docs
*-nonmem
.Rproj.user
.claude/settings.local.json
.claude/worktrees/
# consolidation worktrees created by runner-merge-claude-branches
.worktrees/
# per-task worktrees created inside the repo by Claude Code
.claude-wt/
# parallel vignette-gate output (verify_vignettes_parallel.R)
.vignette_gate.log
.vignette_results.jsonl
data-raw/modeldb-cache.rds
post_merge_advance.sh

# Vignette-gate render artifacts (verify_vignettes_parallel.R)
*.knit.md
vignettes/articles/*.html

# R writes Rplots.pdf automatically whenever base graphics are drawn in a
# non-interactive session (Rscript, devtools::check, a vignette render). It is
# a stray artifact, never content -- but because it is untracked it trips the
# task runner's "uncommitted work left" gate, which failed oare_PMC13176099 on
# three consecutive full-effort dispatches before this was noticed.
Rplots.pdf

# testthat >= 3.3 writes one reprex per failing expectation under R CMD check
# (tests/testthat/_problems/<test>-<line>.R) plus an index (testthat-problems.rds).
# They are diagnostics, never sources; left untracked in a task worktree they
# are counted as uncommitted work by the claude_task_runner guard.
tests/testthat/_problems/
tests/testthat/testthat-problems.rds
# knitr writes figures next to the Rmd when an article is rendered outside
# pkgdown (probe scripts, callr render gates); they are never content, and a
# batch of them was once committed by mistake.
figure/
